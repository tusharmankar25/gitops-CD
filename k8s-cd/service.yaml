apiVersion: v1
kind: Service
metadata:
  name: nodejs-service
  namespace: node-js
spec:
  selector:
    app: nodejs-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
